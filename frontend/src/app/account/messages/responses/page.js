import ResponsesContainer from "@/app/_ui/account/responses/ResponsesContainer";

export const metadata = {
    title: 'Отклики'
}
export default async function Page(){
    return (
        <ResponsesContainer/>
    );
}